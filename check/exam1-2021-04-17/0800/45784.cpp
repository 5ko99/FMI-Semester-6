//СУ "Св. Климент Охридски"
// Факултет по математика и информатика
// Курс Обектно-ориентирано програмиране 2020/21
// Контролно 1
// 
// Име: Данаил
// ФН: 45784
// Специалност: Информатика
// Курс: 1
// Административна група: 4
// Ден, в който се явявате на контролното:  17.04.2021
// Начален час на контролното: <тук попълнете часа за вашата група> 8:00 часа
// Кой компилатор използвате: < Visual studio 2019>
//


#define _CRT_SECURE_NO_WARNINGS 

#include <cassert>
#include <cstring>
#include <iostream>
#include <string>
using namespace std;



ClassRegistration();
{

	Registration(const char* str)

		Registration& operator=(const char* str)

		bool operator==(const char* str) const


		const char* toString() const
}

ClassVehicle;

public:
	vehicle();
	
	void Print Registration()
	{
		cout << "Regnum:  ";
		GetRegnum();
		cout << endl << " ";
		cout << "Registration: ";
		GetRegistration;
		cout << endl << " Make ";
		cout << "Colour: ";
		GetColour();
		cout << endl << " Make ";
		cout << "Model: ";
		GetModel();
		cout << endl << "Registration" ;

	}

	unsigned int GetColour() {
		switch (n_colour) {
			case 1;
				cout << " Red";
				break;
				case  2;
					cout " Blue";
					break;
					case 3;
						cout << "White ";
						break;
					default:
						cout << "Inavalid";
						return false;

						return true;
		}
	}
	void SetColour(unsigned int val = 1) {
		if val > 3;
		cout << " Error ";
	}
 else {
 n_Colour = val;
 }

 unsigned int GetModel() {
	 switch (n_Model) {
	 case 1:
		 cout << "Fiesta ";
		 break;
	 case 2 :
		 cout << "BMW";
		 break;
	 case 3 :
		 cout << "Ford";
		 break;
	 default:
		 cout << "Inavalid";
		 return false;
	 }
	 return true;


	 }

 void SetModel(unsigned int val = 1) {
	 if val > 3;
	 cout << " Error ";
 }
 else {
 n_Model = val;
 }


 void int(const char* Registration);
	
	VehicleList(std::size_t capacity);

	{
		unsigned int Registration{
		case 1;
	cout << "C1234AB ";
	break;
	case 2;
		cout << "XY1111YX";
			break;
		case 3;
			cout << "AB1256";
			break;
			case 4;
				cout << "capacity number of the vehicle";
				break;
			default:
				cout << "Inavalid";
				return false;
		}
		return true;
	}

	void insert(const char* regnum, const char* description)


}
const Vehicle& at(std::size_t index) const


const Vehicle& operator[](std::size_t index) const


bool empty() const
std::size_t capacity() const


std::size_t size() const

const Vehicle* find(const char* regnum) const


~vehicle();
int main()
{
	void Print Registration;
	GetColor();
	GetModel();
	unsigned int Registration;

}








