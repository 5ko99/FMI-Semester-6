#ifndef OBJECT_H
#define OBJECT_H

#include<string>

class Object{
    public:
        // Returns the object to a std::string format according to the method's implementation
        std::string ToString();
};

#endif
